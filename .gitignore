# --- Excluded from the public repository ---

# Python caches
__pycache__/
*.pyc

# Excluded folders
00_overview/
07_report/

# Excluded standalone files
# NOTE: aero_style.py is NOT excluded — it is imported by 01_geometry,
# 02_mesh, 06_postprocessing and the validation scripts, so the published
# pipeline cannot run without it.
case.docx
run_all.py
08_engineering_drawings/draw_engineering.py

# Exclude all PDFs from the project tree...
*.pdf
# ...except the consolidated technical report
!aero_dynamic_stall_report.pdf
