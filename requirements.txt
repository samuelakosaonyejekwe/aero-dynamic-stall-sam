# UNISTALL dynamic-stall case study — runtime dependencies.
#
# The lower bounds below are the versions that first provide every API the
# pipeline calls (numpy: np.trapezoid is optional, the solver binds np.trapz as a
# fallback; pandas 2.1: DataFrame.map, used by the data dossier to blank NaN
# cells -- it was 2.0 here, which does not have that method at all, only the
# applymap it replaced; matplotlib 3.6: Figure.get_layout_engine, used by the
# validation figure). They are minimum-API bounds, NOT a tested matrix: the
# pipeline is exercised end to end only on the versions recorded under
# "Verified on" below.
numpy>=1.24,<3
scipy>=1.10
pandas>=2.1
matplotlib>=3.6

# Report build (07_report/) only — NOT needed to run the solver pipeline.
# On a PEP 668 "externally managed" Python, install these in a venv:
#     python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# or into your own site-packages with:  pip install --user <pkg>
python-docx>=1.0     # assembles case.docx
pillow>=9.0          # image sizing for the consolidated PDF
reportlab>=4.0       # renders case.docx to PDF
PyMuPDF>=1.23        # merges the report body with the dossier and plots album

# The versions the published results were actually produced with are NOT listed
# here. They were, as a hand-written line, and that made them the one claim in
# this study with nothing generating it -- free to drift from the environment the
# artifacts came out of, with no check able to notice. They are recorded by the
# run itself, in 05_solution/runtime_environment.csv (python, platform and one
# version_<pkg> row per library), and verify_invariants.py checks that every
# recorded version satisfies the bounds declared above, that every library the
# run used is declared here, and that none is missing.
